<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="free.puzzle.game.spot.the.difference">

    <application
        android:name="com.trbr.s5differences.AppJava"
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.S5Differences">


        <activity
            android:name="com.trbr.s5differences.Activities.SettingsActivity"
            android:label="@string/title_activity_settings" />

        <activity android:name="com.trbr.s5differences.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.trbr.s5differences.Activities.WinActivity">

        </activity>

        <activity android:name="com.trbr.s5differences.Activities.PlayingActivity"></activity>
    </application>

</manifest>